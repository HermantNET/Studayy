import {
	GET_SUBJECT,
	CREATE_SUBJECT,
	DELETE_SUBJECT,
	EDIT_SUBJECT
} from '../actions/actions.js';
import tasks from './tasks';

export default function (state = [], action) {
	switch (action.type) {
		default:
			return state;
	}
}
