import React from 'react';
import {View, Text, StyleSheet} from 'react-native';
import ms from '../../../masterStyles';

const Record = (props) => {
	return (
		<View style={ms.pageCenter}>
			<Text>Record data here!</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default Record;
